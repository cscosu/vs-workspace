services:
  workspace:
    build: .
    volumes:
      - ./config.yaml:/home/coder/.config/code-server/config.yaml
    ports:
      - "8080:8080"
    command: ["/home/coder/workspace"]
    privileged: true
